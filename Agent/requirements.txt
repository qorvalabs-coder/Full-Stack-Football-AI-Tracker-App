# Core requirements for the AI Football Analysis Agent
ultralytics
supervision
opencv-python-headless
numpy
matplotlib
pandas
celery
redis
sqlalchemy
scikit-learn
psycopg2-binary
pydantic
pydantic-settings

# StrongSort tracker with ReID support — pinned to v10 (stable auto-download API)
# boxmot v19 changed ReID loading and no longer auto-downloads to reid_weights path
boxmot==10.0.72
torch
torchvision

# HTTP client — used to download uploaded videos from the Backend API
# (Backend and Agent run in separate containers with separate volumes)
requests
